id: 00-003
phase: 0
title: Set up code quality tools
description: |
  Configure black, ruff, and mypy for code formatting, linting, and type checking.
dependencies:
  - 00-001
acceptance_criteria:
  - black installed and configured
  - ruff installed with basic rules configured
  - mypy installed and configured for strict type checking
  - pyproject.toml contains tool configurations
  - `black .` runs without errors
  - `ruff check .` runs without errors
  - `mypy jlc_has_it/` runs without errors
estimated_complexity: low
status: completed
notes: |
  Use pyproject.toml for all tool configuration.
  Start with moderate mypy strictness, can increase later.
