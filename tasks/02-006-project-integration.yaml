id: 02-006
phase: 2
title: Implement KiCad project integration
description: |
  Create utilities to update KiCad project files and library tables when
  adding new component libraries.
dependencies:
  - 02-001
acceptance_criteria:
  - ProjectConfig class reads .kicad_pro files
  - Can detect project root from a given path
  - Can update symbol library table (sym-lib-table)
  - Can update footprint library table (fp-lib-table)
  - Creates library directories if needed
  - Tests verify library registration works
estimated_complexity: medium
status: pending
notes: |
  Library tables are S-expression files too.
  Need to handle relative paths correctly.
  Must not break existing library references.
